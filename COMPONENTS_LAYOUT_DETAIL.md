# 컴포넌트 상세 레이아웃 구성 문서

## 📋 목차
1. [GanttGenerator (메인 컨테이너)](#1-ganttgenerator-메인-컨테이너)
2. [FileUploader (파일 업로드)](#2-fileuploader-파일-업로드)
3. [ConfigurationPanel (설정 패널)](#3-configurationpanel-설정-패널)
4. [GenerationStatus (생성 상태)](#4-generationstatus-생성-상태)
5. [GanttPreview (Gantt 미리보기)](#5-ganttpreview-gantt-미리보기)
6. [ThemeProvider (테마 제공자)](#6-themeprovider-테마-제공자)

---

## 1. GanttGenerator (메인 컨테이너)

### 📍 위치
`components/gantt-generator.tsx`

### 🎯 역할
전체 애플리케이션의 최상위 컨테이너 컴포넌트로, 모든 상태 관리와 레이아웃 구조를 담당합니다.

### 📐 레이아웃 구조

```
┌─────────────────────────────────────────────────────────┐
│ Header (고정, flex-shrink-0)                            │
│ ┌───────────────────────────────────────────────────┐   │
│ │ [로고 아이콘 7×7] AGI TR Gantt Generator  v1.0.0 │   │
│ └───────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│ Control Panel (고정, flex-shrink-0, min-h-[70px])       │
│ ┌──────────────┬──────────────┬──────────────┐         │
│ │ FileUploader │ ConfigPanel  │ GenStatus    │         │
│ │ (1/3 그리드)  │ (1/3 그리드)  │ (1/3 그리드)  │         │
│ └──────────────┴──────────────┴──────────────┘         │
├─────────────────────────────────────────────────────────┤
│ Preview Area (flex-1, min-h-0, overflow-y-auto)        │
│ ┌───────────────────────────────────────────────────┐   │
│ │         GanttPreview (전체 영역)                    │   │
│ └───────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 📏 화면 크기 및 반응형

| 요소 | 크기 | 반응형 |
|------|------|--------|
| **전체 컨테이너** | `h-screen` (100vh) | 고정 높이 |
| **Header** | `py-2` (0.5rem 상하) | 고정 |
| **Control Panel** | `min-h-[70px]` | 최소 높이 70px |
| **Grid Layout** | `grid-cols-3 gap-4` | 3열 그리드, 16px 간격 |
| **Preview Area** | `flex-1` (남은 공간 모두) | 유동적 |

### 🎨 스타일링

#### 색상
- **배경**: `bg-background` (OKLCH: 0.985 0 0)
- **Header 배경**: `bg-card` (OKLCH: 1 0 0)
- **Header 하단 테두리**: `border-b border-border`
- **Control Panel 배경**: `bg-muted/30` (30% 투명도)

#### 타이포그래피
- **제목**: `text-base font-semibold` (16px, 600 weight)
- **버전 배지**: `text-xs` (12px)
- **폰트 패밀리**: Catamaran (Sans-serif)

#### 간격
- **Container padding**: `px-4` (좌우 16px)
- **Grid gap**: `gap-4` (16px)
- **Header padding**: `px-4 py-2` (좌우 16px, 상하 8px)

### 🔧 주요 기능

1. **상태 관리**
   - `uploadedFiles`: 업로드된 파일 목록
   - `config`: 프로젝트 설정 (시작일)
   - `isGenerating`: 생성 진행 상태
   - `result`: 생성 결과
   - `error`: 에러 메시지
   - `scheduleData`: 스케줄 데이터

2. **이벤트 핸들러**
   - `handleFilesUploaded`: 파일 업로드 처리
   - `handleGenerate`: Gantt 생성 요청
   - `updateConfig`: 설정 업데이트

3. **API 통신**
   - POST `/api/generate` 호출
   - FormData로 파일 및 설정 전송

---

## 2. FileUploader (파일 업로드)

### 📍 위치
`components/file-uploader.tsx`

### 🎯 역할
TSV/JSON 파일을 드래그 앤 드롭 또는 클릭으로 업로드하고, 파일 목록을 표시합니다.

### 📐 레이아웃 구조

#### Compact 모드 (기본 사용)

```
┌─────────────────────────────────────┐
│ Card Header (py-1.5 px-3)           │
│ ┌─────────────────────────────────┐ │
│ │ [Upload 아이콘 3×3] INPUT FILES │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Card Content (p-2, flex-col)       │
│ ┌─────────────────────────────────┐ │
│ │ Drop Zone (border-dashed)       │ │
│ │ [Upload 3×3] Drop or click      │ │
│ │        (TSV/JSON)              │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ File List (overflow-auto)      │ │
│ │ [FileText] filename [✓] [X]    │ │
│ │ [FileText] filename [✓] [X]    │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 📏 화면 크기 및 반응형

| 요소 | Compact 모드 | 일반 모드 |
|------|-------------|----------|
| **Card 높이** | `h-full` (부모 높이) | `h-full` |
| **Header 높이** | `py-1.5` (6px) | `py-2` (8px) |
| **Header 폰트** | `text-[10px]` (10px) | `text-xs` (12px) |
| **아이콘 크기** | `w-3 h-3` (12px) | `w-3 h-3` (12px) |
| **Drop Zone 패딩** | `p-1.5` (6px) | `p-3` (12px) |
| **Drop Zone 폰트** | `text-[10px]` | `text-xs` / `text-[10px]` |
| **파일 항목 높이** | `py-0.5 px-1` | `p-1.5` |
| **파일 항목 폰트** | `text-[10px]` | `text-xs` |
| **제거 버튼** | `h-4 w-4` (16px) | `h-5 w-5` (20px) |

### 🎨 스타일링

#### 색상
- **Card 배경**: `bg-card`
- **테두리**: `border-border`
- **Header 텍스트**: `text-muted-foreground`
- **Drop Zone (기본)**: `border-border`
- **Drop Zone (드래그 중)**: `border-primary bg-primary/5`
- **Drop Zone (호버)**: `hover:border-muted-foreground`
- **파일 항목 배경**: `bg-muted/50`
- **성공 아이콘**: `text-green-500`
- **에러 텍스트**: `text-destructive`

#### 타이포그래피
- **Header (Compact)**: `text-[10px] font-medium uppercase tracking-wide`
- **Header (일반)**: `text-xs font-medium`
- **Drop Zone 텍스트**: `text-[10px]` 또는 `text-xs`
- **파일명**: `truncate` (말줄임표)

### 🔧 주요 기능

1. **파일 업로드**
   - 드래그 앤 드롭 지원
   - 클릭으로 파일 선택
   - 다중 파일 업로드 (`multiple`)

2. **파일 검증**
   - 확장자: `.tsv`, `.json`, `.txt`
   - 최대 크기: 10MB
   - 파싱 검증

3. **파일 목록 관리**
   - 업로드된 파일 표시
   - 파일 제거 기능
   - 파싱 에러 표시

### 📱 컴포넌트 Props

```typescript
interface FileUploaderProps {
  files: UploadedFile[]
  onFilesChange: (files: UploadedFile[]) => void
  compact?: boolean
}
```

---

## 3. ConfigurationPanel (설정 패널)

### 📍 위치
`components/configuration-panel.tsx`

### 🎯 역할
프로젝트 시작일 등 기본 설정을 관리합니다.

### 📐 레이아웃 구조

#### Compact 모드 (기본 사용)

```
┌─────────────────────────────────────┐
│ Card Header (py-1.5 px-3)           │
│ ┌─────────────────────────────────┐ │
│ │ [Settings 3×3] SETTINGS         │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Card Content (p-2, flex-col)       │
│ ┌─────────────────────────────────┐ │
│ │ [Calendar 2.5×2.5] Project      │ │
│ │         Start Date               │ │
│ │ [Date Input h-7 text-xs]        │ │
│ │ All views sync automatically    │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 📏 화면 크기 및 반응형

| 요소 | Compact 모드 | 일반 모드 |
|------|-------------|----------|
| **Card 높이** | `h-full` | 자동 |
| **Header 높이** | `py-1.5` (6px) | `pb-2` (8px) |
| **Header 폰트** | `text-[10px]` (10px) | `text-sm` (14px) |
| **아이콘 크기** | `w-3 h-3` (12px) | `w-4 h-4` (16px) |
| **Label 폰트** | `text-[10px]` | `text-xs` (12px) |
| **Label 아이콘** | `w-2.5 h-2.5` (10px) | `w-3 h-3` (12px) |
| **Input 높이** | `h-7` (28px) | `h-8` (32px) |
| **Input 폰트** | `text-xs` (12px) | 기본 |
| **Helper 텍스트** | `text-[9px]` (9px) | 없음 |

### 🎨 스타일링

#### 색상
- **Card 배경**: `bg-card`
- **테두리**: `border-border`
- **Header 텍스트**: `text-muted-foreground`
- **Label 텍스트**: `text-muted-foreground`
- **Input 배경**: `bg-muted/50`
- **Input 테두리**: `border-border`
- **Helper 텍스트**: `text-muted-foreground`

### 🔧 주요 기능

1. **프로젝트 시작일 설정**
   - Date input 타입
   - ISO 날짜 형식 (YYYY-MM-DD)
   - 실시간 업데이트

2. **설정 동기화**
   - 모든 뷰에 자동 반영
   - Gantt 차트 날짜 계산에 사용

### 📱 컴포넌트 Props

```typescript
interface ConfigurationPanelProps {
  config: ProjectConfig
  onConfigChange: (config: ProjectConfig) => void
  compact?: boolean
}
```

---

## 4. GenerationStatus (생성 상태)

### 📍 위치
`components/generation-status.tsx`

### 🎯 역할
Gantt 워크북 생성 버튼, 진행 상태, 결과 및 다운로드 링크를 제공합니다.

### 📐 레이아웃 구조

#### Compact 모드 (기본 사용)

```
┌─────────────────────────────────────┐
│ Card Header (py-1.5 px-3)           │
│ ┌─────────────────────────────────┐ │
│ │ [Zap 3×3] GENERATOR             │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Card Content (p-2, flex-col)       │
│ ┌─────────────────────────────────┐ │
│ │ [Generate Button h-7 text-xs]   │ │
│ │   [Play/Loader2] Generate...     │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ Status Area (flex-1)            │ │
│ │ [✓] 3 scenarios, 45 tasks      │ │
│ │ [Download] Download             │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 📏 화면 크기 및 반응형

| 요소 | Compact 모드 | 일반 모드 |
|------|-------------|----------|
| **Card 높이** | `h-full` | `h-full` |
| **Header 높이** | `py-1.5` (6px) | `py-2` (8px) |
| **Header 폰트** | `text-[10px]` (10px) | `text-xs` (12px) |
| **버튼 높이** | `h-7` (28px) | 기본 (sm) |
| **버튼 폰트** | `text-xs` (12px) | 기본 |
| **로더 아이콘** | `w-3 h-3` | `w-3 h-3` |
| **성공 아이콘** | `w-2.5 h-2.5` (10px) | `h-3 w-3` (12px) |
| **상태 텍스트** | `text-[9px]` (9px) | `text-[10px]` (10px) |
| **다운로드 버튼** | `h-6 text-[10px]` | `text-xs` |

### 🎨 스타일링

#### 색상
- **Card 배경**: `bg-card`
- **테두리**: `border-border`
- **Header 텍스트**: `text-muted-foreground`
- **버튼 (활성)**: Primary 색상
- **버튼 (비활성)**: Disabled 상태
- **성공 배지**: `text-green-600`
- **성공 Alert**: `border-green-500/50 bg-green-500/10`
- **에러 텍스트**: `text-destructive`
- **에러 Alert**: Destructive variant

### 🔧 주요 기능

1. **생성 버튼**
   - 파일 업로드 여부에 따라 활성/비활성
   - 생성 중 로딩 상태 표시
   - Play 아이콘 / Loader2 스피너

2. **상태 표시**
   - 성공: 시나리오 수, 작업 수 표시
   - 에러: 에러 메시지 표시
   - 빈 상태: "Upload files to begin" 메시지

3. **다운로드 링크**
   - 생성 성공 시 다운로드 버튼 표시
   - 파일명 표시 (일반 모드)
   - Download 아이콘

#### Compact 모드 다운로드 버튼
- **높이**: `h-6` (24px)
- **폰트**: `text-[10px]` (10px)
- **아이콘**: `w-2.5 h-2.5` (10px)
- **배경**: `bg-transparent`

#### 일반 모드 Alert
- **성공 Alert**: `border-green-500/50 bg-green-500/10 py-2`
- **아이콘**: `h-3 w-3 text-green-500`
- **텍스트**: `text-[10px] text-green-600`
- **에러 Alert**: `variant="destructive"`, `py-2`
- **아이콘**: `h-3 w-3`
- **텍스트**: `text-[10px]`

### 📱 컴포넌트 Props

```typescript
interface GenerationStatusProps {
  isGenerating: boolean
  error: string | null
  result: GenerationResult | null
  onGenerate: () => void
  hasFiles: boolean
  compact?: boolean
}
```

---

## 5. GanttPreview (Gantt 미리보기)

### 📍 위치
`components/gantt-preview.tsx`

### 🎯 역할
5가지 탭 뷰(Gantt Chart, Table View, Voyage Summary, Documents, Summary)를 제공하여 스케줄 데이터를 시각화하고 문서를 관리합니다.

### 📐 레이아웃 구조

```
┌─────────────────────────────────────────────────────────┐
│ Card Header (pb-3, border-b)                            │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [BarChart3] Schedule Preview                        │ │
│ │ [Database] Fixed/Uploaded [Zoom] [Badge] [Badge]   │ │
│ └─────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│ Tabs Header (px-4 pt-3, bg-muted/30)                    │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [Gantt Chart] [Table View] [Voyage] [Docs] [Summary]│ │
│ └─────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│ Tabs Content (flex-1, min-h-0)                           │
│                                                          │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Gantt Chart Tab                                      │ │
│ │ ┌─────────────────┬──────────────────────────────┐  │ │
│ │ │ Timeline Header │ Month Row / Day Row          │  │ │
│ │ │ (고정)          │ (고정, 스크롤 동기화)          │  │ │
│ │ ├─────────────────┼──────────────────────────────┤  │ │
│ │ │ Activity Names  │ Gantt Bars                   │  │ │
│ │ │ (세로 스크롤)   │ (가로/세로 스크롤)            │  │ │
│ │ └─────────────────┴──────────────────────────────┘  │ │
│ └─────────────────────────────────────────────────────┘ │
```

### 📏 화면 크기 및 반응형

#### Header 영역

| 요소 | 크기 |
|------|------|
| **Card Header 높이** | 자동 (`pb-3`) |
| **제목 폰트** | `text-sm font-semibold` (14px, 600) |
| **제목 아이콘** | `w-4 h-4` (16px) |
| **버튼 높이** | `h-7` (28px) |
| **버튼 폰트** | `text-xs` (12px) |
| **아이콘 버튼** | `h-7 w-7` (28px) |
| **아이콘 크기** | `h-3 w-3` (12px) |
| **Badge 폰트** | `text-xs font-medium` |

#### Tabs 영역

| 요소 | 크기 |
|------|------|
| **Tabs Header 높이** | `h-8` (32px) |
| **Tabs Header 패딩** | `px-4 pt-3` (좌우 16px, 상 12px) |
| **Tab Trigger 높이** | `h-7` (28px) |
| **Tab Trigger 폰트** | `text-xs` (12px) |
| **Tab Trigger 패딩** | `px-3` (좌우 12px) |
| **Tab 아이콘** | `w-3 h-3` (12px) |

#### Gantt Chart 영역

| 요소 | 크기 |
|------|------|
| **Left Panel 너비** | `w-80` (320px, 고정) |
| **Timeline Header 높이** | `h-16` (64px, 고정) |
| **Month Row 높이** | `h-8` (32px) |
| **Day Row 높이** | `h-8` (32px) |
| **Cell 너비 (줌 레벨)** | 24px / 32px / 48px / 64px |
| **Group Header 높이** | `h-10` (40px) |
| **Task Row 높이** | `h-9` (36px) |
| **Gantt Bar 높이** | `h-5` (20px) |
| **폰트 (Month)** | `text-xs font-semibold` |
| **폰트 (Day)** | `text-[10px]` |
| **폰트 (Activity)** | `text-xs` |

#### 로딩 및 빈 상태

**로딩 상태**:
- **컨테이너**: `h-96` (384px)
- **스피너**: `w-12 h-12` (48px), `border-4 border-primary border-t-transparent`, `animate-spin`
- **텍스트**: `text-sm text-muted-foreground` - "Generating schedule..."

**빈 상태**:
- **컨테이너**: `h-96` (384px)
- **아이콘 배경**: `w-16 h-16 bg-muted rounded-full`
- **아이콘**: `w-8 h-8 text-muted-foreground` (BarChart3)
- **제목**: `text-sm font-medium` - "No Preview Available"
- **설명**: `text-xs text-muted-foreground` - "Upload files and generate to see the schedule preview"

#### Header Badge
- **Activities Badge**: `variant="secondary"`, `text-xs font-medium`
- **Days Badge**: `variant="outline"`, `text-xs font-medium`
- **간격**: `gap-3` (12px)

### 🎨 스타일링

#### 색상

**Trip 그룹별 색상**:
- **AGI TR Units 1-2** (Sky):
  - Header: `bg-sky-600 dark:bg-sky-700`
  - Row: `bg-sky-50/50 dark:bg-sky-950/20`
  - Bar: `bg-sky-500`
  - Border: `border-sky-600`

- **AGI TR Units 3-4** (Emerald):
  - Header: `bg-emerald-600 dark:bg-emerald-700`
  - Row: `bg-emerald-50/50 dark:bg-emerald-950/20`
  - Bar: `bg-emerald-500`
  - Border: `border-emerald-600`

- **AGLI TR Units 5-6** (Amber):
  - Header: `bg-amber-600 dark:bg-amber-700`
  - Row: `bg-amber-50/50 dark:bg-amber-950/20`
  - Bar: `bg-amber-500`
  - Border: `border-amber-600`

- **AGL TR Unit 7** (Violet):
  - Header: `bg-violet-600 dark:bg-violet-700`
  - Row: `bg-violet-50/50 dark:bg-violet-950/20`
  - Bar: `bg-violet-500`
  - Border: `border-violet-600`

**일반 색상**:
- **배경**: `bg-background`
- **Card 배경**: `bg-card`
- **테두리**: `border-border`
- **주말 배경**: `bg-muted`
- **주말 텍스트**: `text-muted-foreground`

#### 타이포그래피
- **제목**: `text-sm font-semibold`
- **Month 헤더**: `text-xs font-semibold`
- **Day 헤더**: `text-[10px]`
- **Activity 이름**: `text-xs`
- **Group 라벨**: `text-sm font-bold`

### 🔧 주요 기능

1. **줌 컨트롤**
   - 4단계 줌 레벨: 24px, 32px, 48px, 64px
   - ZoomIn / ZoomOut 버튼
   - 최소/최대 제한

2. **데이터 소스 전환**
   - Fixed Data / Uploaded Data 토글
   - Database 아이콘 표시

3. **그룹 접기/펼치기**
   - Trip 그룹별 토글
   - ▶ / ▼ 아이콘 표시

4. **스크롤 동기화**
   - Left Panel ↔ Right Panel (세로)
   - Timeline Header ↔ Right Panel (가로)

5. **주말 하이라이트**
   - 주말 날짜 배경색 변경
   - 텍스트 색상 변경

6. **툴팁**
   - Gantt Bar 호버 시 상세 정보 표시
   - TooltipProvider 사용

7. **Voyage Summary**
   - 마일스톤 추적 (4개)
   - 조수 정보 통합
   - 날씨 정보 통합
   - SHAMAL 경고

8. **Deadline 오버레이**
   - "Deadlines" 버튼으로 토글
   - 타임라인 헤더에 세로선 표시
   - Gantt 바 영역에 세로선 표시
   - Risk 레벨별 색상 구분 (ON_TRACK/AT_RISK/OVERDUE)
   - 줌 레벨 변경 시 위치 자동 조정

9. **Documents 탭**
   - Voyage 선택 그리드
   - 문서 체크리스트 (카테고리별)
   - Workflow 상태 관리
   - 마감일 추적 및 Due state 표시

10. **Docs Progress Overlay**
    - **위치**: Trip group header row 위에 오버레이
    - **기능**:
      - 문서 진행률 표시 (Approved/Total 비율)
      - Progress bar + Badge (`Docs X/Y`)
      - 클릭 시 Docs 탭으로 이동 + 해당 Voyage 자동 선택
      - 키보드 접근성 지원 (Tab, Enter/Space)
      - 포커스 링 스타일 (focus-visible)
    - **통합**: `components/gantt-preview.tsx`의 Trip group header row에 삽입
    - **매칭**: Voyage의 `tripGroupKey`와 Gantt group의 `activityId2` 매칭

11. **trip-groups.ts** (공용 상수)
    - **위치**: `lib/voyage/trip-groups.ts`
    - **책임**: Trip 그룹 정의 및 필터링 상수 제공
    - **내용**:
      - `TRIP_GROUPS_BY_ACTIVITY_ID2`: Trip 그룹 정의 배열 (4개 그룹)
        - AGI TR Units 1-2 (Sky)
        - AGI TR Units 3-4 (Emerald)
        - AGLI TR Units 5-6 (Amber)
        - AGL TR Unit 7 (Violet)
      - `VALID_TRIP_ACTIVITY_ID2`: Voyage 필터링용 허용 목록
    - **사용처**:
      - `lib/voyage/derive-voyages.ts`: Voyage 추출 시 필터링 (실제 Trip 그룹 4개만 표시)
      - `components/gantt-preview.tsx`: Trip 그룹 색상 매핑
    - **필터링 로직**: "SPMT", "MARINE" 등 다른 `activityId2`는 자동 제외하여 Voyage 4개만 생성

#### Gantt Chart 탭 추가 세부사항

**Group Header 기간 표시**:
- **위치**: Group Header 내부, 절대 위치
- **스타일**: `bg-white/30 rounded-sm`, `h-6` (24px)
- **텍스트**: `text-[10px] text-white font-medium`
- **내용**: "{totalDuration} days"
- **계산**: Group의 시작일/종료일 기준

**Task Row 상세**:
- **높이**: `h-9` (36px)
- **패딩**: `paddingLeft: 12px`
- **텍스트**: `text-xs text-foreground/80`

**Gantt Bar 상세**:
- **높이**: `h-5` (20px)
- **패딩**: `px-1.5` (6px)
- **텍스트 표시 조건**: `duration > 2`일 때만 작업명 표시
- **텍스트 스타일**: `text-[9px] text-white font-medium truncate`
- **호버 효과**: `hover:opacity-80 transition-opacity`

**Tooltip 내용**:
- **최대 너비**: `max-w-xs`
- **내용**:
  - 작업명: `font-medium text-xs`
  - Trip 라벨: `text-[10px] text-muted-foreground`
  - 날짜 범위: `text-[10px]`
  - 기간: `text-[10px]`

**주말 하이라이트**:
- **위치**: Task row 내부, 절대 위치
- **스타일**: `bg-muted/30`
- **계산**: 각 날짜의 `isWeekend` 속성 기반

### Table View 탭

#### 레이아웃 구조

```
┌─────────────────────────────────────────────────────┐
│ Table Container (p-4)                              │
│ ┌─────────────────────────────────────────────────┐ │
│ │ Table (border rounded-lg)                       │ │
│ │ ┌─────────────────────────────────────────────┐ │ │
│ │ │ Header (sticky top-0, bg-muted/50)          │ │ │
│ │ │ Trip │ Activity │ Start │ End │ Duration   │ │ │
│ │ ├─────────────────────────────────────────────┤ │ │
│ │ │ Row (hover:bg-muted/30)                     │ │ │
│ │ │ [Badge] name │ date │ date │ days          │ │ │
│ │ └─────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### 테이블 구조
- **전체 패딩**: `p-4` (16px)
- **테이블 폰트**: `text-xs` (12px)
- **Header**: `sticky top-0`, `bg-muted/50`
- **Header 셀**: `p-2`, `border-b border-border`, `font-semibold`
- **Body 셀**: `p-2`, `border-b border-border/50`
- **Row 호버**: `hover:bg-muted/30`

#### 컬럼 구성
1. **Trip**: Badge로 표시, Trip 그룹 색상 적용
2. **Activity Name**: 작업명
3. **Start Date**: `text-muted-foreground`
4. **End Date**: `text-muted-foreground`
5. **Duration**: `text-right`, "{duration} days"

#### Badge 스타일
- **폰트**: `text-[10px]`
- **배경**: Trip 그룹의 `header` 색상
- **텍스트**: Trip 그룹의 `headerText` 색상
- **테두리**: `border-0`

### Voyage Summary 탭

#### 레이아웃 구조

```
┌─────────────────────────────────────────────────────┐
│ Container (p-4 space-y-4)                          │
│ ┌─────────────────────────────────────────────────┐ │
│ │ Legend (flex gap-4, text-xs)                    │ │
│ │ [Waves] Tide │ [Wind] Weather │ [Alert] SHAMAL  │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ Table (border rounded-lg)                       │ │
│ │ Trip │ # │ Milestone │ Date │ Tide/Weather     │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### 레전드 (Legend)
- **컨테이너**: `flex items-center gap-4 text-xs text-muted-foreground`
- **Tide 아이콘**: `w-3 h-3 text-blue-500` (Waves)
- **Weather 아이콘**: `w-3 h-3 text-cyan-500` (Wind)
- **SHAMAL 아이콘**: `w-3 h-3 text-amber-500` (AlertTriangle)

#### 테이블 구조
- **전체 패딩**: `p-4` (16px)
- **간격**: `space-y-4` (16px)
- **테이블 폰트**: `text-xs` (12px)
- **Header**: `sticky top-0`, `bg-muted/50`

#### 컬럼 구성
1. **Trip** (`w-24`):
   - `rowSpan={VOYAGE_MILESTONES.length}` (4개 마일스톤)
   - Trip 그룹 색상 적용 (`header`, `headerText`)
   - **내용**:
     - Trip 라벨: `font-bold`
     - Units: `text-[10px] opacity-80`

2. **#** (`w-8`):
   - 마일스톤 번호 (1-4)
   - **스타일**: `w-5 h-5 rounded-full`, Trip 그룹 `bar` 색상
   - **텍스트**: `text-white text-[10px] font-bold`

3. **Milestone**: 마일스톤 라벨

4. **Date** (`w-28`):
   - `text-muted-foreground`
   - 날짜 없으면 "—" 표시

5. **Tide / Weather** (`w-48`):
   - **조수 정보** (Load-out 마일스톤):
     - Waves 아이콘: `w-3 h-3 text-blue-500`
     - 높이: `{max_height_m}m`
     - Risk Badge: `text-[9px]`
     - 시간대: `text-[10px] text-muted-foreground`
   - **날씨 정보** (Sail-away 마일스톤):
     - Wind 아이콘: `w-3 h-3 text-cyan-500`
     - 풍속: `{wind_max_kn}kn`
     - 파고: `{wave_max_m}m`
     - Risk Badge: `text-[9px]`
     - SHAMAL 경고: `w-3 h-3 text-amber-500` (조건부)
   - 정보 없으면 "—" 표시

#### Risk Badge 색상
- **LOW**: `bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400`
- **MEDIUM**: `bg-amber-100 text-amber-800 dark:bg-amber-950/30 dark:text-amber-400`
- **HIGH**: `bg-red-100 text-red-800 dark:bg-red-950/30 dark:text-red-400`

### Documents 탭

#### 레이아웃 구조

```
┌─────────────────────────────────────────────────────┐
│ Container (p-4 space-y-4)                          │
│ ┌─────────────────────────────────────────────────┐ │
│ │ VoyageMiniGrid (grid-cols-4 gap-3)            │ │
│ │ [Voyage Card] [Voyage Card] [Voyage Card] ... │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ DocumentChecklist                               │ │
│ │ ┌─────────────────────────────────────────────┐ │ │
│ │ │ Category Accordion                          │ │ │
│ │ │ ┌─────────────────────────────────────────┐ │ │ │
│ │ │ │ [Checkbox] Document Name                │ │ │ │
│ │ │ │ [Badge] Priority [Badge] Due State     │ │ │ │
│ │ │ │ Due Date | Progress                     │ │ │ │
│ │ │ └─────────────────────────────────────────┘ │ │ │
│ │ └─────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### VoyageMiniGrid

**위치**: `components/documents/voyage-mini-grid.tsx`

**기능**:
- Voyage 선택 카드 그리드 (4열)
- 선택된 Voyage 강조 표시 (ring-2 ring-primary)
- Overdue 문서 개수 표시 (destructive badge)
- 마일스톤 날짜 표시 (Arrival, Doc Deadline)

**레이아웃**:
- **그리드**: `grid-cols-4 gap-3` (4열, 12px 간격)
- **카드 패딩**: `p-3` (12px)
- **카드 높이**: 자동
- **제목 폰트**: `text-sm font-semibold`
- **날짜 폰트**: `text-xs text-muted-foreground`
- **Overdue Badge**: `variant="destructive"`, `text-[10px]`

**상태 관리**:
- `useVoyageContext`를 통해 선택된 Voyage 관리
- 클릭 시 `setSelectedVoyageId` 호출

#### DocumentChecklist

**위치**: `components/documents/document-checklist.tsx`

**기능**:
- **하이브리드 레이아웃**: 카드 뷰 ↔ 테이블 뷰 토글 (레이아웃 버튼)
- **카테고리별 문서 그룹화**: 좌측 카테고리 네비게이션 (테이블 뷰) 또는 Accordion (카드 뷰)
- **문서 체크리스트 표시**: 카드 뷰 (기존) 또는 테이블 뷰
- **상태 머신**: `not_started → submitted → approved` 전이 (Submit/Approve 버튼)
- **Workflow 상태 표시**: Status Badge 및 상태 라벨
- **Due state 표시**: on_track/at_risk/overdue (색상 Badge)
- **마감일 계산**: anchor milestone + offset days (calendar/business days)
- **D-카운트다운**: 마감일까지 남은 일수 표시 (카드/테이블 뷰)
- **진행률 표시**: Progress bar (카테고리별)
- **History 자동 추가**: 상태 변경 시 자동 기록

**레이아웃 모드**:
- **카드 뷰** (기본):
  - 카테고리별 Card 그룹 (Accordion 스타일)
  - 체크박스로 approved 상태 토글 (`not_started` ↔ `in_progress` ↔ `approved`)
  - D-카운트다운 Badge 표시 (D-N, Due today, Overdue Nd)
  - Due state 색상 Badge (on_track/at_risk/overdue)
  - Priority Badge (critical/important/standard/recommended)
- **테이블 뷰**:
  - 좌측: 카테고리 탭 (Tabs, 4열 중 일부)
  - 우측: 문서 테이블 (Document, Due, Status, Action 컬럼)
  - Submit/Approve 버튼으로 상태 전이 (상태 머신 가드 적용)
  - Reset/Reopen 버튼 (조건부 표시: submitted일 때 Reset, approved일 때 Reopen)
  - D-카운트다운 + Due state Badge
  - 카테고리별 진행률 표시 (approved/total)

**상태 머신**:
- **전이 경로**: `not_started` → `submitted` → `approved`
- **가드 로직**: `canTransition()` 함수로 전이 허용 여부 확인
  - `not_started`에서만 "Submit" 액션 가능
  - `submitted`에서만 "Approve" 또는 "Reset" 액션 가능
  - `approved`에서만 "Reopen" 액션 가능
- **전이 함수**: `transitionStatus()` - 상태 전이 수행
  - Submit: `not_started` → `submitted`
  - Approve: `submitted` → `approved`
  - Reset: `submitted` → `not_started` (제출 취소)
  - Reopen: `approved` → `submitted` (재검토)
- **History 자동 추가**: `updateDoc`에서 상태 변경 시 자동 기록 (`STATE_${STATUS}`)
- **라벨 표시**: `statusLabel()` - 상태를 읽기 쉬운 텍스트로 변환

**데이터 소스**:
- `data/doc-templates.json`: 문서 템플릿 정의 (카테고리, 우선순위, 마감일 규칙)
- `lib/documents/deadline-engine.ts`: 마감일 계산 엔진 (calendar/business days)
- `lib/documents/workflow.ts`: 상태 전이 로직 (canTransition, transitionStatus, statusLabel)
- `contexts/voyage-context.tsx`: 문서 상태 관리 (localStorage 동기화)

**마감일 계산 로직**:
- **Anchor Milestone**: Voyage의 마일스톤 날짜 (예: `mzp_arrival`)
- **Offset Days**: 마일스톤으로부터의 상대 일수 (음수=이전, 양수=이후)
- **Offset Type**: `calendar_days` (주말 포함) 또는 `business_days` (주말 제외)
- **자동 재계산**: 일정 변경 시 마감일 자동 재계산
- **Due State**: `on_track` (여유), `at_risk` (2일 이내), `overdue` (지남)

**하이브리드 레이아웃 사용 가이드**:
- **카드 뷰**: 빠른 체크, 전체 개요, 카테고리별 진행률 확인
- **테이블 뷰**: 상세 관리, Submit/Approve 워크플로우, 카테고리별 필터링
- **전환**: 상단 우측 버튼으로 전환
- **반응형**: 모바일(카드 뷰 권장), 태블릿(둘 다 가능), 데스크톱(테이블 뷰 권장)
- 자세한 내용은 [`docs/DOCUMENT_WORKFLOW_GUIDE.md`](../docs/DOCUMENT_WORKFLOW_GUIDE.md) 참조

**카테고리**:
1. PTW Land / HM / HSE / MZ Ops Submission Pack
2. Loading Cargo Related Documents
3. AD Maritime NOC Documents

**문서 항목 구조**:
- **체크박스**: Workflow 상태 표시 (카드 뷰)
- **문서명**: Template title
- **Priority Badge**: critical/important/standard/recommended
- **Due State Badge**: on_track/at_risk/overdue
- **마감일**: 계산된 마감일 표시
- **D-카운트다운**: D-N, Due today, Overdue Nd
- **진행률**: 완료된 항목 대비 Progress bar

**하이브리드 레이아웃 사용 가이드**:

#### 레이아웃 전환
- 상단 우측에 "Card View" / "Table View" 버튼
- 활성 뷰: `default` variant (강조)
- 비활성 뷰: `outline` variant
- 기본값: 카드 뷰

#### 카드 뷰 사용 시나리오
- 빠른 체크 및 승인
- 전체 문서 개요 확인
- 카테고리별 진행률 확인
- 간단한 승인/미승인 토글

#### 테이블 뷰 사용 시나리오
- 상세 관리 및 상태 전이
- Submit/Approve 워크플로우
- 카테고리별 필터링
- 마감일 중심 관리

**History 이벤트 일관성**:
- 모든 상태 변경 이벤트는 `STATE_${STATUS}` 형식으로 통일
- 예: `STATE_SUBMITTED`, `STATE_APPROVED`
- `updateDoc`에서 자동 추가 (상태 변경 감지)

**카드 뷰 D-카운트다운 개선**:
- Badge로 표시 (기존 텍스트에서 개선)
- Overdue: `variant="destructive"` + 빨간색 강조
- Due today: `variant="default"` + 노란색
- Normal: `variant="secondary"` 또는 `variant="outline"`
- Overdue 시 AlertTriangle 아이콘 표시

**마감일 계산 로직**:

#### Anchor Milestone
문서 마감일은 Voyage의 주요 이벤트 날짜를 기준으로 계산됩니다:

```typescript
type MilestoneKey =
  | "mzp_arrival"      // MZP 도착일
  | "mzp_departure"    // MZP 출발일
  | "loadout_start"    // 적재 시작일
  | "loadout_end"      // 적재 완료일
  | "agi_arrival"      // AGI 도착일
  | "agi_departure"    // AGI 출발일
  | "doc_deadline"     // 문서 제출 마감일 (명시적)
```

#### Offset Days
- **음수** (`-4`): Anchor 이전 4일
- **양수** (`+2`): Anchor 이후 2일
- **0**: Anchor 당일

#### Offset Type
1. **calendar_days**: 주말 포함 (단순 날짜 더하기)
2. **business_days**: 주말 제외 (토/일 제외)

#### 계산 예시

**예시 1: Calendar Days**
```json
{
  "anchor": {
    "milestoneKey": "mzp_arrival",
    "offsetDays": -4,
    "offsetType": "calendar_days"
  }
}
```
- MZP Arrival: `2026-01-26` (화요일)
- 계산: `2026-01-26 - 4일 = 2026-01-22` (금요일)
- 결과: `2026-01-22`

**예시 2: Business Days**
```json
{
  "anchor": {
    "milestoneKey": "loadout_start",
    "offsetDays": -3,
    "offsetType": "business_days"
  }
}
```
- Load-out Start: `2026-01-29` (금요일)
- 계산: 주말 제외하고 3일 전
- 결과: `2026-01-26` (화요일)

#### Due State 계산
```typescript
const daysUntilDue = (dueDate - today) / (1000 * 60 * 60 * 24)

if (daysUntilDue < 0) return "overdue"      // 지난 경우
if (daysUntilDue <= 2) return "at_risk"     // 2일 이내
return "on_track"                            // 여유 있음
```

**특수 케이스**:
- `workflowState === "approved"` 또는 `"waived"` → 항상 `"on_track"`

#### 자동 재계산
일정 변경 시:
1. Gantt 일정 업데이트
2. Voyage 마일스톤 재추출
3. 문서 마감일 자동 재계산
4. Due State 자동 업데이트

**스타일링**:
- **카테고리 Accordion**: `w-full`
- **문서 항목**: `flex items-center gap-3 p-3 border-b`
- **Priority Badge**:
  - critical: `variant="destructive"`
  - important: `variant="default"`
  - standard: `variant="secondary"`
  - recommended: `variant="outline"`
- **Due State Badge**:
  - on_track: `bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400`
  - at_risk: `bg-amber-100 text-amber-800 dark:bg-amber-950/30 dark:text-amber-400`
  - overdue: `bg-red-100 text-red-800 dark:bg-red-950/30 dark:text-red-400`

**데이터 소스**:
- `data/doc-templates.json`: 문서 템플릿 정의
- `lib/documents/deadline-engine.ts`: 마감일 계산 엔진
- `contexts/voyage-context.tsx`: 문서 상태 관리 (localStorage)

### Summary 탭

#### 레이아웃 구조

```
┌─────────────────────────────────────────────────────┐
│ Container (p-4 space-y-4)                          │
│ ┌─────────────────────────────────────────────────┐ │
│ │ Trip Statistics (grid-cols-4 gap-4)            │ │
│ │ [Card] [Card] [Card] [Card]                   │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ Project Timeline Card                           │ │
│ │ Start │ [Timeline Bar] │ End                   │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### Trip 통계 카드
- **그리드**: `grid-cols-4 gap-4` (4개 Trip)
- **카드 구조**:
  - 상단 색상 바: `h-2`, Trip 그룹 `header` 색상
  - 내용: `pt-3 pb-3`
  - **표시 내용**:
    - 활동 수: `text-2xl font-bold`
    - Units: `text-xs text-muted-foreground`
    - 기간: `text-[10px] text-muted-foreground mt-1`

#### Project Timeline
- **Card Header**: `py-3`, `text-sm` - "Project Timeline"
- **Timeline Bar**:
  - 컨테이너: `flex-1 mx-4 h-2 bg-muted rounded-full overflow-hidden relative`
  - Trip별 세그먼트: `h-full`, Trip 그룹 `bar` 색상, `absolute`
  - 위치 계산: 시작일/종료일 기준 퍼센트
- **날짜 표시**:
  - Start: `text-xs`, `text-muted-foreground` / `font-medium`
  - End: `text-right`, `text-xs`, `text-muted-foreground` / `font-medium`

### 📱 컴포넌트 Props

```typescript
interface GanttPreviewProps {
  scheduleData: ScheduleData | null
  config: ProjectConfig
  isGenerating: boolean
}
```

---

## 6. ThemeProvider (테마 제공자)

### 📍 위치
`components/theme-provider.tsx`

### 🎯 역할
Next.js의 `next-themes`를 사용하여 다크 모드 지원을 제공합니다.

### 🎨 스타일링

#### 색상 시스템 (OKLCH)

**라이트 모드** (`:root`):
- Background: `oklch(0.985 0 0)` - 거의 흰색
- Foreground: `oklch(0.145 0 0)` - 거의 검은색
- Primary: `oklch(0.55 0.15 250)` - 보라색 계열
- Muted: `oklch(0.96 0.005 250)` - 연한 회색
- Border: `oklch(0.91 0.005 250)` - 밝은 회색

**다크 모드** (`.dark`):
- Background: `oklch(0.12 0.01 250)` - 어두운 배경
- Foreground: `oklch(0.95 0 0)` - 밝은 텍스트
- Primary: `oklch(0.65 0.15 250)` - 밝은 보라색
- Muted: `oklch(0.22 0.01 250)` - 어두운 회색
- Border: `oklch(0.26 0.01 250)` - 어두운 테두리

### 🔧 주요 기능

1. **다크 모드 지원**
   - 시스템 설정 자동 감지
   - 수동 토글 가능

2. **테마 전환**
   - 부드러운 전환 애니메이션
   - CSS 변수 기반

---

## 📊 전체 레이아웃 요약

### 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│ Header (고정, ~40px)                                     │
├─────────────────────────────────────────────────────────┤
│ Control Panel (고정, ~70px)                              │
│ [FileUploader] [ConfigurationPanel] [GenerationStatus] │
├─────────────────────────────────────────────────────────┤
│ GanttPreview (유동, 나머지 공간)                          │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Header + Tabs (~100px)                              │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ Content (flex-1)                                    │ │
│ │                                                     │ │
│ │                                                     │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 폰트 시스템

- **Sans-serif**: Catamaran (기본)
- **Mono**: Geist Mono
- **폰트 크기**: 9px ~ 16px (주로 10px, 12px, 14px)

### 색상 팔레트

- **Primary**: 보라색 계열 (OKLCH: 0.55 0.15 250)
- **Success**: 초록색 (#22c55e)
- **Warning**: 노란색/주황색
- **Error**: 빨간색 (Destructive)
- **Trip 그룹**: Sky, Emerald, Amber, Violet

---

**문서 버전**: 1.0.0
**최종 업데이트**: 2025-01-19
```

